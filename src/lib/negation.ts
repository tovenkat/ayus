// Stub — negation detection (not needed for phr2 basic extraction)

export function detectTestNegation(_input: Record<string, unknown>): { isNegated: boolean; trigger: string | null } {
  return { isNegated: false, trigger: null };
}
